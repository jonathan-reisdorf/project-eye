@echo off
echo *** Setting the path for VS (only necessary if you want to compile sth, for instance when you run npm install within gazejs folder)
echo If something does not work here, please adapt the path in this file to your VS installation path and make sure that your global PATH variable does not contain any quotation chars!
call "C:\Program Files (x86)\Microsoft Visual Studio 12.0\VC\vcvarsall.bat" x86